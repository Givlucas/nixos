# Base host configuration
# Minimal host with no specific settings - used as foundation
{ ... }:

{
  # Base host has no hostname or specific configuration
  # It inherits everything from the base module
}
